1. [DONE] bytecode VM — lazy OnceLock compilation, stack-based interpreter, tensor
   indexing (Index), nested lambdas (MakeClosure), non-recursive Def::Func in blocks.

   Still possible to add to the bytecode VM:
   1a. Slices in compiled bodies: T[lo..hi] currently falls back; add a Slice instruction
   1b. Ranges in compiled bodies: lo..hi currently falls back; produces a 1-D tensor
   1c. Tensor literals: (1,2; 3,4) currently falls back; straightforward to compile
   1d. Recursive local functions: needs MakeRecFn or a Cell-based two-pass trick
   1e. Loop instruction: bounded iteration (sum(f, lo, hi) style) compiled to a flat
       loop in the VM — also the only recursion-analogue that is GPU-safe
   1f. Move Instruction enum to src/vm.rs so src/gpu/ can import it without pulling
       in all of eval.rs (deferred until eval.rs is split, see item 9)
   1g. LoadBuffer instruction: GPU-side tensor indexing for captured buffers inside
       GPU lambda bodies (v2 GPU feature, see CONSIDERATIONS.md §15)

2. GPU backend (in progress, see CONSIDERATIONS.md)

9. Split eval.rs — it is too large; defer until the GPU eval path (gpu_eval.rs) is
   designed, so the split boundary is clear.

17. [PARTIAL] MXFR protocol: added channels field (C=1 scalar, C=3 RGB).
    Rgba32Float texture path in renderer; shader RGB passthrough branch.
    Still TODO: showRGB(T) and animateRGB(T) mathlang builtins for [H,W,3]
    and [t,H,W,3] tensors.

11. aur package that puts animator and m executables in bin; hdf5 option?

18. consider moving graph() and animate2D() to !graph and !animate2D since theyre not really functions

19. graph() doesnt open the animator but that was a feature we supposedly implemented
20. matrix printing is still broken in the repl:
> tensor(1, 2, 2)
result = ⎡ 0  0 ⎤
⎣ 0  1 ⎦

21.1. [PARTIAL] builtins have signatures (builtin_sig) shown by !type/!help. General
   !help text cleaned up (type vocabulary + colon return syntax). Dedicated per-builtin
   help texts (`!help <builtin>` from a help directory) DEFERRED — too many tokens.

21.2. [DONE] !type infers and fuses signatures. A function shows `(t1, t2) -> ret`
   (no f() prefix, no var names); the return type is inferred from the body via
   signature fusion when not annotated. e.g. f(x: real, y: complex) = y*linspace(0,1,10)
   gives `(real, complex) -> complex tensor`. See infer_type() in eval.rs.

21.3. [DONE] type keywords highlight green (only after a ':' so `tensor` the builtin
   stays purple but `: tensor` the annotation is green). See highlight_line in repl.rs.

21.4. [DONE] !type reports the TYPE, not the value: !type 2 -> real, !type pi -> real,
   x=5; !type x -> real, !type 3+2i -> complex. Falls back to static inference for
   arbitrary expressions.

21.5. [DONE] return-type syntax is now `f(): real = 1` (colon after the param list),
   and lambdas `(x: real): real -> body`. The old `f() -> real = 1` arrow form is gone.

22. e constant was wrong (2.718281828281828); now std::f64::consts::E. [DONE]
