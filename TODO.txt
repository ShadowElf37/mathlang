1. [DONE] bytecode VM — lazy OnceLock compilation, stack-based interpreter, tensor
   indexing (Index), nested lambdas (MakeClosure), non-recursive Def::Func in blocks.

   Still possible to add to the bytecode VM:
   1a. Slices in compiled bodies: T[lo..hi] currently falls back; add a Slice instruction
   1b. Ranges in compiled bodies: lo..hi currently falls back; produces a 1-D tensor
   1c. Tensor literals: (1,2; 3,4) currently falls back; straightforward to compile
   1d. Recursive local functions: needs MakeRecFn or a Cell-based two-pass trick
   1e. Loop instruction: bounded iteration (sum(f, lo, hi) style) compiled to a flat
       loop in the VM — also the only recursion-analogue that is GPU-safe
   1f. Move Instruction enum to src/vm.rs so src/gpu/ can import it without pulling
       in all of eval.rs (deferred until eval.rs is split, see item 9)
   1g. LoadBuffer instruction: GPU-side tensor indexing for captured buffers inside
       GPU lambda bodies (v2 GPU feature, see CONSIDERATIONS.md §15)

2. GPU backend (in progress, see CONSIDERATIONS.md)

3. [DONE] !savenpy <var> <file.npy> / !loadnpy <var> <file.npy>
   Saves real tensors as float64, complex tensors as complex128.
   Loads f2/f4/f8, c8/c16, i/u 1/2/4/8 bytes, bool — converts all to f64.

4. Consider removing : syntax for the final expression in blocks and the CLI — it is
   synonymous with ; for the last expression, and tuple separation after : could jus
   be written explicitly by the user. Maybe defer this change until we need : for something else.

5. Claude Opus exhaustive ergonomics and bug check

6. [DONE] animator error messages: show type+shape instead of full tensor dump; hint
   about NaN/division-by-zero when a complex tensor is returned unexpectedly.

7. [DONE] animator: spacebar to pause/resume; left/right arrows to step frames (also pause).

8. Type hinting enforced by the interpreter (fn, tensor, real, complex?)

9. Split eval.rs — it is too large; defer until the GPU eval path (gpu_eval.rs) is
   designed, so the split boundary is clear.

10. graphing should display the graph instead of saving to file?

11. aur package that puts animator and m executables in bin; hdf5 option?

12. [DONE] replaced quit/exit with !q, !quit, !exit

13. [DONE] matrix printing: 2D+ tensors inside tuples now get a newline prefix so
    the matrix rows don't run into the surrounding text.

14. [DONE] removed old 1D-tuple fft; renamed fftn/ifftn to fft/ifft (n-D tensor DFT).

15. [DONE] animator: --title <text> CLI flag; shown as top-left text overlay with
    drop-shadow. From mathlang: set WGPU_TITLE env var before calling animate2D.
