1. eigenvalues and eigenvectors

2. accept [] for tensor literals
   > rand(2)
   result = [0.272, 0.073]
   > result = [0.272, 0.073]    ← this should work, currently errors on LBracket

3. ctrl+d still doesnt work when something is already typed in the stdin in the repl, only works when stdin is empty
