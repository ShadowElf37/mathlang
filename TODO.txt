1. you were in the middle of making n-D fftn and ifftn, and support for complex numbers inside tensors; continue

2. write a skill file that you can read to make yourself faster and better at writing .math files
3. write a heat.math with a heat solver. there should be a function of t that outputs 2D temperature grids once set up (maybe a heatSolver that outputs a lambda of t from initial conditions). initial conditions to test: a 2D disk of high conductance in "air" of low conductance, where the air is hot (lets say 500K) and the disk is cold (lets say 250K) at the beginning. this should be in pure mathlang. if you encounter trouble anywhere, or anything is really awkward, mark this as a new bug/feature.
4. write an animate2D() that takes either f(t) or a 3D block, using the new realtime animation binary i will give you when it is time to approach this TODO item
5. eigenvalues and eigenvectors
