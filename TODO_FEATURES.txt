1. [PARTIAL] bytecode VM — lazy OnceLock compilation, stack-based interpreter, tensor
   indexing (Index), nested lambdas (MakeClosure), non-recursive Def::Func in blocks.

   Still possible to add to the bytecode VM:
   1a. Slices in compiled bodies: T[lo..hi] currently falls back; add a Slice instruction
   1b. Ranges in compiled bodies: lo..hi currently falls back; produces a 1-D tensor
   1c. Tensor literals: (1,2; 3,4) currently falls back; straightforward to compile
   1d. Recursive local functions: needs MakeRecFn or a Cell-based two-pass trick
   1e. [DONE v0.21.1] Loop instruction: bounded iteration (sum/prod/iterate/scan)
       compiled to a flat loop in the VM (Instruction::Loop(LoopForm, argc)) — the
       only recursion-analogue that is GPU-safe. The compiler pushes the operands
       (function + bounds/seed/count) and emits one Loop; run_vm drains them and
       calls the shared Val-based cores (agg_vals/iterate_vals/scan_vals), so
       results and errors are identical to tree-walk. These no longer force a
       fallback when used inside a compiled lambda body. (integral/deriv/map/filter/
       reduce still fall back — they need unevaluated args / bespoke handling.)
   1f. [DONE v0.21.1] Moved the Instruction enum (+ new LoopForm) to src/vm.rs; it
       depends only on crate::ast (Op, Expr), never on Val, so src/gpu/ can import
       it without pulling in eval.rs. run_vm/Compiler stay in eval.rs (they need Val).
   1g. LoadBuffer instruction: GPU-side tensor indexing for captured buffers inside
       GPU lambda bodies (v2 GPU feature, see CONSIDERATIONS.md §15)

2. [SEPARATE BRANCH] GPU backend (see CONSIDERATIONS.md) + PDE solvers

2.5. Split eval.rs — it is too large (now 4662 lines); defer until the GPU eval path
   (gpu_eval.rs) is designed, so the split boundary is clear.

3. [PARTIAL] MXFR protocol: added channels field (C=1 scalar, C=3 RGB).
    Rgba32Float texture path in renderer; shader RGB passthrough branch.
    Still TODO: showRGB(T) and animateRGB(T) mathlang builtins for [H,W,3]
    and [t,H,W,3] tensors.

4. aur package that puts animator and m executables in bin; hdf5 option?

5.1 [DONE v0.23.3] clean up !help — dramatically shrunk (namespace details now under !help <ns>),
    per-namespace descriptions, !helpdef <ns> <text> for user namespaces; BUILTIN_FNS cleaned
    to only reflect names actually in the flat Env::new table.
5.2 [DONE v0.26.0] tensor(<field>) converts a field to a plain tensor (component
    data; 0-form → grid-shaped, vector field keeps trailing component axis). Geometry
    dropped (rebuild with field()); returning (tensor,dx) rejected as it breaks composition.
5.2.1 [DONE v0.26.0] field(f, lo, hi, counts, bc [, metric]) function constructor —
    samples f at physical grid coords x_a = lo_a + i·dx_a (same dx the field uses),
    mirroring tensor(f,…) but with proper centre/spacing. No intermediate tensor needed.
5.3 (resolved by partial deriv?) single axis ops.grad(field, axis)?
5.4 [DONE v0.23.3] ~ operator for logical not (mirrors bits.not); bare `not` now errors (namespace-only).
    Operator chains (~~x) work. BUILTIN_FNS no longer lists bits-namespace names.

6. [DONE v0.22.0] Tame the builtin namespace — done via Rust-backed namespaces with
   `.` syntax instead of the prelude-extraction route below. Implemented (see
   src/ns/, docs/CODEBASE.md §Namespaces):
   - Val::Namespace + Expr::Member (Token::Dot in the parser postfix loop); the VM
     falls back to tree-walk on Member.
   - Standard namespaces registered by force in Env::new (crate::ns::register_all):
     special, bits, stats, linalg, vec (relocated niche builtins, exposed as
     Val::Builtin("<bare>") so the giant eval_builtin match is untouched), plus
     operators/solver (NEW PDE functions implemented in their own module files,
     routed from eval_builtin via crate::ns::dispatch).
   - Curated relocation (kept common names flat — inv det solve fft ifft mean std
     norm sort roll shift eig eigvals …): the moved names left both the flat
     Env::new table and is_protected, so they are no longer reserved words.
   - User namespaces: `!namespace <name>` at the top of an included .math file +
     `private` to keep a definition internal (import_file / NsBuild).
   The original two-stage plan (6a prelude extraction in .math, 6b additive
   namespace values) is kept below for history; we did the Rust-namespace variant
   of 6b directly (and went beyond it by relocating, not just aliasing).

   6a. [PRIMARY] Prelude extraction. Move the "library tier" builtins — special
       functions and niche stats (erf, erfc, j0/j1, jinc, gaussian_cdf, sinc,
       maybe median/mode, the integrators once stable) — out of Rust into an
       auto-loaded mathlang prelude (.math), built on the MATHLANG_INIT / import_file
       machinery that already exists. Keep a small frozen *core* protected (~40
       names: arithmetic, tensor construction/indexing, cell/get/set, the loop
       forms iterate/scan/sum/prod, if, map/filter/reduce). Net effect: the Rust
       builtin table and is_protected both shrink dramatically, AND prelude-defined
       names become *user-overridable* (they're ordinary defs, not protected) — a
       strict ergonomics win. No grammar change; only an auto-load hook + moving
       definitions. This is the high-value/low-risk move and should land first.

   6b. [OPTIONAL, later] Additive namespace values, only if 6a leaves discoverability
       wanting. Expose grouped modules — linalg, stats, signal, bits — as first-class
       Val::Namespace map values so `linalg.det(A)` works and a namespace can be bound
       or passed around. Keep all current flat names working (back-compat); new niche
       builtins then go *only* into a namespace, capping global growth. Feasibility:
       the lexer already errors on a bare `.` not followed by a digit (src/lexer.rs
       ~88) and disambiguates decimals by digit-lookahead, so `.` is free for member
       access with no grammar conflict. Needs: a Val::Namespace variant, a parse node
       for `ident.ident` access, and resolution in eval + the VM (LoadCaptured-style).
       Do NOT move existing names *out* of the global table (that breaks every script);
       namespaces are an additive alias layer, not a replacement.

   Rationale: 6a is the honest dividend of "is the infra good enough to build in
   mathlang" — yes, so push the library tier into mathlang itself. 6b is the
   long-term shape for a browsable stdlib but shouldn't gate 6a.


CONTINUAL FOR CLAUDE OPUS:

0.1. Try to tackle a complex project in pure mathlang, and see what's too difficult, awkward, unintuitive, verbose, messy, unperformant, etc. from the perspective of an engineer/physicist/mathematician -- mark those as new bugs or feature requests
0.2. Search codebase for performance optimizations, bugs, unwise features, excessively delicate or complex code, technical debt and dead code, very old features that dont fit the style of the language, bad ergonomics or things that "should work" but don't, poor or outdated documentation, idiotic ideas
0.3. Recommend features that would improve the language, consistent with the style and goals of the language so far
0.4. Particularly consider where things can become more unified and generalized
0.5. Generate a markdown report with your findings and opinions
