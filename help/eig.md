## eig

Computes eigenvalues and eigenvectors of a square matrix. Returns a tuple (eigenvalues, eigenvectors).

**Examples:**
```
> eig((4,1; 1,3))
result = ([4.618..., 2.381...], [[...], [...]])
```
