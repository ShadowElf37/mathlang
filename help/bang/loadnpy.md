## !loadnpy

Load a NumPy `.npy` file into a variable. Supports dtypes: `f2`, `f4`, `f8` (real), `c8`, `c16` (complex), integer types, and bool.

**Usage:** `!loadnpy <var> <file.npy>`

**Examples:**
```
> !loadnpy T data.npy
loaded T (100 elements, real) from data.npy
```
