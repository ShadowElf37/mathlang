tuples become "vectors", or 1-tensors

we will have general tensor support with multidimensional indexing e.g.
M[(1, 2)]
perhaps we must get rid of tuple[1, 2] syntax for picking out items? or put that in a new function pick(tuple, (i1, i2,...))?

we will have einstein notation sum support somehow... einsum(M, N, "ij, ik") is close to how numpy does it... M.ij * N.jk ? what do you think? i want it to be super slick.

M = (1, 2, 3; 4, 5, 6; 7, 8, 9)
but then 3-tensor support is lacking

this allows us to create coordinate grids
X = ((0, 0), (1, 0), (2, 0); (0, -1), (1, -1), ...; ...)

there should be builtins for:
zeros((n, n...))
ones((n, n...))
lingrid((startx, starty...), (endx, endy...), (nx, ny...))
Identity(n)

matrix operations
vector operations (generalized to matrices too, e.g. norm and Frobenius norm unified)

tensor-valued names can be recognized in the repl and bolded automatically. capitalized names for matrices are standard, like Identity, Spin, Rotation, etc

it is difficult to construct custom matrices without mutability and empty(). then you can call empty() and fill a matrix somehow. or maybe we have a slick definition like M.ij = f(i, j) to solve this problem.


multidimensional graphing; animations
maybe we want a custom SDL 2D grapher to play realtime animations?
